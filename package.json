{
  "name": "supportcue",
  "version": "1.0.0",
  "description": "AI-powered multi-tenant customer support platform with RAG-grounded chat and human escalation",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --omit=dev --prefix backend",
    "start": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "seed:superuser": "npm run seed:superuser --prefix backend",
    "reset:superuser": "npm run reset:superuser --prefix backend",
    "setup:vector-index": "npm run setup:vector-index --prefix backend"
  }
}
