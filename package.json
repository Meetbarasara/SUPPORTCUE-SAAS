{
  "name": "supportcue",
  "version": "1.0.0",
  "description": "AI-powered multi-tenant customer support platform with RAG-grounded chat and human escalation",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18 <23"
  },
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install --omit=dev",
    "start": "cd backend && node server.js",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "seed:superuser": "cd backend && npm run seed:superuser",
    "reset:superuser": "cd backend && npm run reset:superuser",
    "setup:vector-index": "cd backend && npm run setup:vector-index"
  }
}
